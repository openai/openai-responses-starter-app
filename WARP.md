# WARP

This placeholder file exists to satisfy build environments that attempt to read `WARP.md` during Next.js compilation. It is not used by the application at runtime.

If you are using Cloud or CI builds that reference this file, you can replace this with internal documentation as needed.
